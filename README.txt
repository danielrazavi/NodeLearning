`nodemon app` to run the whole thing.

`npm install` to get all dependencies in order.

per the routes mentioned in the code, see what things can be done with this MEXN Stack (has no front end library like react being used at all.)